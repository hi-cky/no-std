# 🚀 系统启动汇编代码
# 
# 这是系统的入口点，负责：
# - 设置栈指针
# - 调用 Rust 初始化函数

    .section .text.entry
    .globl _start
_start:
    # 设置栈指针到栈顶
    la sp, __STACK_TOP
    # 调用 main 函数
    call main